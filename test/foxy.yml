build:
  context: ./server
  steps:
    - export TEST=Yo
    - cd server && go build main.go
    - echo hey $TEST

run:
  context: ./server
  watch: true
  steps:
    - go run main.go

test:
  context: ./server
  env_file: ./test.env
  env:
    MYVAR: test value
  steps:
    - go run main.go
